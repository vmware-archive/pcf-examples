# abort script on any command that exits with a non zero value
set -eu

mkdir ${BOSH_INSTALL_TARGET}/bin
echo "echo 'hello'" > ${BOSH_INSTALL_TARGET}/bin/service-adapter
chmod +x ${BOSH_INSTALL_TARGET}/bin/service-adapter


#cd ${BOSH_INSTALL_TARGET}
#tar xzvf ${BOSH_COMPILE_TARGET}/adapter/adapter.tgz


#exit 1

#mvn package

#cp target/*.jar ${BOSH_INSTALL_TARGET}/adapter.jar


#echo "set -e
#java -jar ${BOSH_INSTALL_TARGET}/adapter.jar $@" > ${BOSH_INSTALL_TARGET}/bin/service-adapter

#chmod +x ${BOSH_INSTALL_TARGET}/bin/service-adapter

